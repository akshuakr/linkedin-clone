import React from "react";
import styled from "styled-components";

const Righside = (props) => {
    return <Container>Rightside</Container>;
};

const Container = styled.div`
    grid-area: rightside;
`;

export default Righside;
